# Copy to .env (gitignored). docker-compose reads it automatically.

POSTGRES_DB=familysplit
POSTGRES_USER=familysplit
POSTGRES_PASSWORD=familysplit

# pgAdmin (only used when running `docker compose --profile tools up`)
PGADMIN_EMAIL=admin@familysplit.com
PGADMIN_PASSWORD=admin
