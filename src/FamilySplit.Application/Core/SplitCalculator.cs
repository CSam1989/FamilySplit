namespace FamilySplit.Application.Core;

/// <summary>
/// Computes the per-participant CalculatedAmount for an expense based on
/// non-excluded weight_snapshots. Rounding remainder (max ±0.01) is applied
/// to the largest-share participant. Implementation lands in Phase 5.
/// </summary>
public class SplitCalculator
{
    // Methods added in Phase 5 (Expenses — core).
}
