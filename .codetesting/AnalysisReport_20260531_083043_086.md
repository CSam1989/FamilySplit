# Test Failures due possible code bugs

## FamilySplit.UnitTests.csproj - SubscribeAsync_ReturnsTrue_WhenFullFlowSucceeds
- **Confidence**: High
- **Test File**: tests\FamilySplit.UnitTests\Services\PushNotificationClientServiceTests.cs
- **Bug Location**: src/FamilySplit.Client/Services/PushNotificationClientService.cs@118

### Analysis
The test is skipped because BrowserSubscription is a private nested record inside PushNotificationClientService, making it impossible to instantiate from the test project. The InternalsVisibleTo attribute for FamilySplit.UnitTests is already configured in the Client project, so changing the visibility from 'private' to 'internal' would allow the test to construct the record and mock the JS return value.

### Suggested Fix
In `src/FamilySplit.Client/Services/PushNotificationClientService.cs` at line 118, change `private record BrowserSubscription` to `internal record BrowserSubscription`:
```csharp
internal record BrowserSubscription(string Endpoint, string P256dh, string Auth);
```
The `InternalsVisibleTo` for `FamilySplit.UnitTests` is already configured in the `.csproj`.

