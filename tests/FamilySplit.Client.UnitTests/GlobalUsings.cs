global using FluentAssertions;
global using Xunit;
// Note: do NOT add `global using Bunit;` here — Bunit.TestContext clashes with
// Xunit.TestContext (introduced in xunit v3). Add `using Bunit;` locally in each
// component test file that needs it.
